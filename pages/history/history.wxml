<!--pages/history/history.wxml-->
<view class="container">
  <!-- Header -->
  <view class="header">
    <text class="header-title">📜 历史记录</text>
    <text class="header-subtitle">共 {{history.length}} 条问答</text>
  </view>

  <!-- History List -->
  <view class="history-list" wx:if="{{history.length > 0}}">
    <view 
      class="history-item card" 
      wx:for="{{history}}" 
      wx:key="id"
      bindtap="onItemTap"
      data-item="{{item}}"
    >
      <view class="item-header">
        <text class="item-q-icon">Q</text>
        <text class="item-question">{{item.question}}</text>
      </view>
      <view class="item-preview">
        <text class="item-a-icon">A</text>
        <text class="item-reply">{{item.reply}}</text>
      </view>
      <view class="item-footer">
        <text class="item-time">{{item.createTime}}</text>
        <text class="item-arrow">›</text>
      </view>
    </view>
  </view>

  <!-- Empty State -->
  <view class="empty-state" wx:else>
    <text class="empty-icon">📭</text>
    <text class="empty-text">暂无历史记录</text>
    <text class="empty-subtext">去问狗头军师几个问题吧！</text>
    <button class="btn-primary empty-btn" bindtap="goToAsk">去提问</button>
  </view>
</view>
