<!--pages/history/history.wxml-->
<view class="page-container">
  <!-- 顶部导航栏 -->
  <view class="sticky-header">
    <view class="header-inner">
      <view class="back-btn" bindtap="goBack">
        <text class="back-arrow">←</text>
      </view>
      <text class="header-title">提问历史</text>
    </view>
  </view>

  <!-- 历史列表 -->
  <view class="history-list" wx:if="{{questions.length > 0}}">
    <view class="card history-item" wx:for="{{questions}}" wx:key="_id" bindtap="onItemTap" data-item="{{item}}">
      <text class="history-question">{{item.question}}</text>
      <view class="history-meta">
        <text class="history-time">{{item.timeText}}</text>
        <text class="history-likes" wx:if="{{item.likes > 0}}">❤️ {{item.likes}}</text>
      </view>
    </view>
  </view>

  <!-- 空状态 -->
  <view class="empty-state" wx:else>
    <text class="empty-icon">📭</text>
    <text class="empty-text">还没有提问记录</text>
    <text class="empty-hint">去问问狗头军师吧！</text>
    <view class="btn-primary empty-btn" bindtap="goToAsk">
      <text>去提问</text>
    </view>
  </view>
</view>
