<!--pages/result/result.wxml-->
<view class="page-container">
  <!-- 顶部导航栏 -->
  <view class="sticky-header">
    <view class="header-inner">
      <view class="back-btn" bindtap="goBack">
        <text class="back-arrow">←</text>
      </view>
      <text class="header-title">问答详情</text>
    </view>
  </view>

  <!-- 用户信息卡片 -->
  <view class="card user-card">
    <view class="user-left">
      <text class="user-emoji">🎭</text>
      <view class="user-info">
        <text class="user-name">匿名用户</text>
        <text class="user-time">{{formatTimeText}}</text>
      </view>
    </view>
    <view class="user-right">
      <text class="view-icon">👁</text>
      <text class="view-count">{{viewCount}}</text>
    </view>
  </view>

  <!-- 问题卡片 -->
  <view class="card question-card">
    <text class="card-label">问：</text>
    <text class="question-text">{{question}}</text>
  </view>

  <!-- 回答卡片 -->
  <view class="card answer-card">
    <view class="answer-header">
      <text class="answer-emoji">🐶</text>
      <text class="answer-label">军师说：</text>
    </view>
    <text class="answer-text">{{reply}}</text>
  </view>

  <!-- 操作栏 -->
  <view class="card action-bar">
    <view class="action-item" bindtap="handleLike">
      <text class="action-icon {{isLiked ? 'liked' : ''}}">{{isLiked ? '❤️' : '🤍'}}</text>
      <text class="action-count {{isLiked ? 'liked' : ''}}">{{likeCount}}</text>
    </view>
    <view class="action-item" bindtap="handleComment">
      <text class="action-icon">💬</text>
      <text class="action-count">{{commentCount}}</text>
    </view>
    <view class="action-item" bindtap="handleShare">
      <text class="action-icon">↗</text>
      <text class="action-count">分享</text>
    </view>
  </view>

  <!-- 评论占位 -->
  <view class="card comment-placeholder">
    <text class="comment-emoji">💬</text>
    <text class="comment-hint">评论功能即将上线...</text>
  </view>

  <!-- Toast -->
  <view class="toast {{showToast ? 'show' : ''}}" wx:if="{{showToast}}">
    <text class="toast-text">{{toastText}}</text>
  </view>
</view>
